name: CodeQL

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  actions: read
  checks: write
  contents: read
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Initialize CodeQL
        id: initialize
        uses: github/codeql-action/init@e0ea141027937784e3c10ed1679e503fcc2245bc # v3.28.11
        with:
          queries: security-extended 
          languages: TypeScript
          source-root: src

      - name: Autobuild
        id: autobuild
        uses: github/codeql-action/autobuild@e0ea141027937784e3c10ed1679e503fcc2245bc # v3.28.11

      - name: Perform CodeQL Analysis
        id: analyze
        uses: github/codeql-action/analyze@e0ea141027937784e3c10ed1679e503fcc2245bc # v3.28.11
